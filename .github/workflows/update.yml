name: Data Update

on:
  schedule:
    # Cheap live-hash preflight every 15 minutes; heavy extraction runs only on change.
    - cron: '*/15 * * * *'
  workflow_dispatch:
    inputs:
      force:
        description: 'Force data regeneration even when the remote version is unchanged'
        required: false
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: "data-update"
  cancel-in-progress: false

jobs:
  check_live:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.live.outputs.changed }}
      product_version: ${{ steps.live.outputs.product_version }}
      bundle_hash: ${{ steps.live.outputs.bundle_hash }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Check official live resource version
        id: live
        run: python scripts/check_live_version.py

  update-data:
    needs: check_live
    if: >-
      always() &&
      (github.event_name == 'workflow_dispatch' ||
      needs.check_live.outputs.changed == 'true')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23.4'

      - name: Install protocol documentation tools
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler
          go install github.com/pseudomuto/protoc-gen-doc/cmd/protoc-gen-doc@v1.5.1
          echo "$HOME/go/bin" >> "$GITHUB_PATH"

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Update data
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.force }}" = "true" ]; then
            python main.py
          else
            python main.py --skip-if-current
          fi

      - name: Validate generated data
        run: |
          python -m unittest discover -s tests -v
          python -m compileall -q main.py src tests
          python - <<'PY'
          import json
          from pathlib import Path

          count = 0
          for root in (Path("data"), Path("raw")):
              for path in root.rglob("*.json"):
                  json.loads(path.read_text(encoding="utf-8"))
                  count += 1
          print(f"json ok: {count}")
          PY
          files=$(find data/protocol/proto -maxdepth 1 -name '*.proto' -printf '%f\n' | sort)
          protoc --proto_path=data/protocol/proto --descriptor_set_out=/tmp/majsoul_protocol.desc $files

      - name: Audit live display resources
        run: python scripts/audit_resources.py

      - name: Commit and push changes
        id: commit_changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add -A data/ raw/
          if git diff --staged --quiet; then
            echo "No changes to commit"
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            git commit -m "Update data"
            git push
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Copy and rename majsoul_protocol.desc
        run: cp /tmp/majsoul_protocol.desc ./liqi.desc

      - name: Run get_max_data.py
        id: get_max_data
        run: python get_max_data.py

      - name: Get latest release
        id: get_latest_release
        uses: joutvhu/get-release@v1
        with:
          latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

      - name: Upload release
        uses: softprops/action-gh-release@v3
        # The tag includes bundle_hash, so a bundle-only official update always
        # creates a distinct immutable Release even when docs/Unity stay equal.
        if: |
          steps.get_latest_release.outcome == 'failure' ||
          steps.get_latest_release.outputs.tag_name != steps.get_max_data.outputs.version
        with:
          tag_name: ${{ steps.get_max_data.outputs.version }}
          token: ${{ secrets.GITHUB_TOKEN }}
          body: |
            Release ${{ steps.get_max_data.outputs.version }}
            Auto-generated max_data.yaml and liqi.desc files.
          make_latest: true
          files: |
            max_data.yaml
            liqi.desc
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
